var numeral = -80;

if (numeral > 0) {
	var resultado = numeral * 10;
	console.log("el resultado es:" + resultado);
} else if (numeral < 0) {
	var resultado = numeral / 10;
	console.log("el resultado es:" + resultado);
} else {
	var resultado = numeral + 10;
	console.log("el resultado es:" + resultado);
}